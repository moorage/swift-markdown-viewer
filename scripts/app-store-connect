#!/bin/zsh

set -euo pipefail

source "$(cd "$(dirname "$0")" && pwd)/lib/xcode-env.sh"

export ROOT_DIR
export BUNDLE_IDENTIFIER

python3 "$(cd "$(dirname "$0")" && pwd)/lib/app_store_connect.py" "$@"
